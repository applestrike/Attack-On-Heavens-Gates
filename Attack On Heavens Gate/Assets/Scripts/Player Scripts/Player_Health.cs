using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Player_Health : Base_Health
{
    //Player_FlashOnHit player_FlashOnHit;

    //Player_UI player_UI;

    //private void Awake()
    //{
    //    player_FlashOnHit = GetComponent<Player_FlashOnHit>();
    //    player_UI = GetComponent<Player_UI>();
    //}

    //public override void DecreaseHealth(int change)
    //{
    //    if (currentHealth >= 0)
    //    {
    //        player_FlashOnHit.CallFlashOnHit();

    //        currentHealth -= change;

    //        if (currentHealth <= 0)
    //        {
    //            currentHealth = 0;
    //            CheckForDeath();
    //        }

    //        player_UI.UpdateHealthBar();
    //    }
    //}
}
